export const PLAN_SELECT_COLUMNS = `
  id,
  user_id,
  title,
  event_type,
  status,
  guest_count,
  budget_cap_cents,
  neighborhood,
  date_window_start,
  date_window_end,
  ticketed,
  ticketing_model,
  food_responsibility,
  venue_terms,
  agent_action,
  profit_goal_cents,
  notes,
  excluded_cuisines,
  excluded_vendor_attributes,
  preferred_vendor_attributes,
  plan_revision_count,
  committed_venue_id,
  committed_venue_quoted_price_cents,
  committed_venue_quoted_deal_model,
  committed_venue_quoted_terms,
  committed_venue_at,
  committed_vendors,
  metadata,
  created_at,
  updated_at
`

export const PLAN_MESSAGE_SELECT_COLUMNS = `
  id,
  plan_id,
  role,
  content,
  message_type,
  metadata,
  created_at
`

export const RECOMMENDATION_SELECT_COLUMNS = `
  id,
  plan_id,
  type,
  reference_id,
  external_name,
  price_cents,
  notes,
  rank,
  is_best_fit,
  status,
  superseded_at,
  superseded_by_revision_id,
  plan_revision_at_creation,
  metadata,
  created_at
`

export const APPROVAL_SELECT_COLUMNS = `
  id,
  plan_id,
  agent_action_id,
  action_label,
  provider,
  event_date,
  price_cents,
  fees_cents,
  refund_terms,
  cancellation_terms,
  package_details,
  delivery_email,
  payment_method_id,
  status,
  requested_amount_cents,
  authorized_amount_cents,
  authorized_by,
  authorized_at,
  approved_by,
  approved_at,
  expires_at,
  snapshot_hash,
  superseded_at,
  superseded_by_revision_id,
  superseded_reason,
  created_at,
  updated_at
`

export const VENUE_OPPORTUNITY_BRIEF_SELECT_COLUMNS = `
  id,
  plan_id,
  organizer_user_id,
  title,
  event_type,
  event_components,
  guest_count,
  date_window_start,
  date_window_end,
  time_preference,
  neighborhood,
  budget_cents,
  must_haves,
  requested_terms,
  deposit_target_cents,
  status,
  summary,
  requirements,
  budget_range_cents,
  date_window,
  response_deadline,
  approval_status,
  outreach_message,
  created_at,
  updated_at
`

export const VENUE_OPPORTUNITY_INVITE_SELECT_COLUMNS = `
  id,
  opportunity_id,
  brief_id,
  target_type,
  venue_id,
  vendor_profile_id,
  status,
  is_claimed,
  route_to_concierge,
  match_score,
  capacity_fit,
  budget_fit,
  requirement_fit,
  proposed_deposit_cents,
  quoted_price_cents,
  venue_response_json,
  magic_link_token,
  magic_link_expires_at,
  viewed_at,
  response_at,
  response_payload,
  admin_notes,
  sent_at,
  responded_at,
  expires_at,
  created_at,
  updated_at
`
