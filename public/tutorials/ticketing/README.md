# Ticketing Tutorial Videos

Place how-to videos for the builder ticketing setup cards in this folder.

Suggested filenames:

- `eventbrite-account-connect.mp4`
- `posh-webhook-setup.mp4`
- `luma-webhook-setup.mp4`

After the files are added, wire them into `components/auth/TicketingSetupGuide.tsx`.
