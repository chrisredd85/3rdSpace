#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run linter
npm run lint

# Type check
npm run type-check

# Run tests (pass when no tests match — worktrees exclude .claude/ paths in jest config)
npm run test -- --passWithNoTests
