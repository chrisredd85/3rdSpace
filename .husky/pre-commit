#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run linter
npm run lint

# Type check
npm run type-check

# Run tests deterministically in pre-commit. The full Jest suite has known
# timing flakes under parallel workers in local worktrees.
npm run test -- --passWithNoTests --runInBand
